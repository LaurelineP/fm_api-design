{
  "name": "api_express-postgre-prisma",
  "version": "1.0.0",
  "description": "Using  - Node / JavaScript - Express - PostgreSQL / Prisma - Deploying on render. - TS",
  "main": "server/index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon server",
    "start-vanilla": "nodemon server vanilla",
    "start-express": "nodemon server express",
    "build": "rimraf ./dist && tsc",
    "start:build": "node dist/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.8.10",
    "nodemon": "^3.0.1",
    "prisma": "^5.5.2",
    "rimraf": "^5.0.5",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "@prisma/client": "^5.5.2",
    "@types/express": "^4.17.20",
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  },
  "include": "**/*",
  "exclude": [
    "dist"
  ]
}